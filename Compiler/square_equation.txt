{
"Программа для решения квадратных уравнений
Введите a:
"
IN -[i]-> a;
"Введите b:
"
IN -[i]-> b;
"Введите c:
"
IN -[i]-> c;
d = b * b - 4 * a * c;
"Дискриминант:
"
d -[I]-> OUT;"
"

!?! (d > 0) {
  x = (b * (0 - 1) + d ^ 0.5) / (2 * a);
  y = (b * (0 - 1) - d ^ 0.5) / (2 * a);
"Два корня:
"
x -[I]-> OUT;"
"
y -[I]-> OUT;"
"
}

!?! (d == 0) {
  x = (b * (0 - 1)) / (2 * a);
"Один корень:
"
x -[I]-> OUT;"
"
}
!?! (d < 0) {
"Корней нет.
"
}
}
